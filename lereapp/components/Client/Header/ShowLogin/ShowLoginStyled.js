const styles = (theme) => ({
  mainShow: {
    position: "absolute",
    top: 20,
    zIndex: 10,
    width: 200,
    height: 100,
    background: "#FFFFFF",
    boxShadow: "0px 4px 16px rgba(0, 0, 0, 0.16)",
    borderRadius: 8,
  },
  mainShowBody: {
    paddingTop: 20,
    paddingLeft: 16,
    paddingBottom: 20,
    display: "flex",
    flexDirection: "column",
  },
});

export default styles;